# Base requirements (CPU-only heuristic execution)
# Usage: pip install -r requirements.txt
# For GPU features: pip install -r requirements-gpu.txt
# For LLM features: pip install -r requirements-llm.txt
drain3==0.9.11
watchdog==6.0.0
requests==2.32.3
pyyaml==6.0.2
pytest==8.3.4
