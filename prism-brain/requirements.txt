# base (always)
drain3==0.9.11
watchdog==6.0.0
requests==2.32.3
pyyaml==6.0.2
pytest==8.3.4
# optional [gpu]  -> transformers torch  # comment: pip install -r requirements.txt --no-deps for CPU
# optional [llm]  -> ollama
